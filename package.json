{
  "name": "auto-tag",
  "version": "1.13.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist && mkdir ./dist",
    "test": "echo \"No tests here!\"",
    "build": "npm run clean && touch ./dist/hello.js",
    "preversion": "npm test",
    "version": "npm run build && git add -A ./dist -f",
    "postversion": "git push && git push --tags && rm -rf ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dp-lewis/auto-tag.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dp-lewis/auto-tag/issues"
  },
  "homepage": "https://github.com/dp-lewis/auto-tag#readme"
}
